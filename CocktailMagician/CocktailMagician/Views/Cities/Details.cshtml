@model CityViewModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>@Model.Name</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Bars)
        </dt>
        <dd class="col-sm-10">
            @if (Model.Bars.Count != 0)
            {
                @string.Join(", ", Model.Bars.Select(b => b.Name));
            }
            else
            {
                @Html.DisplayName("N/A");
            }
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Cocktail Magician"))
    {
        <a style="margin: 0 2px;" class="btn btn-outline-info" href="/Cities/Edit/@Model.Id">Edit</a>
        <a style="margin: 0 2px;" class="btn btn-outline-info" href="/Cities/Delete/@Model.Id">Delete</a>
    }
    <a style="margin: 0 2px;" class="btn btn-outline-info" href="/Cities/Index/">Back to List</a>
</div>
